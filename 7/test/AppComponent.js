import App from '../src/component/App';
import {BrowserRouter} from 'react-router-dom';

describe('AppComponent', () => {
    it('render', () => {
        <BrowserRouter><App PlayerName='Amir'/></BrowserRouter>;
    });
});
